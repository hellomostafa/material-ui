import React from 'react';
import Typography from '@mui/material/Typography'

const Create = () => {
    return (
        <div>
            <Typography 
                variant="h1"
                color="primary"
                align="center"
            >
                Create a New Note
            </Typography>
        </div>
    );
};

export default Create;